import { HeaderContainer } from './Header.styles'

export const Header = () => {
  return (
    <HeaderContainer>
      <span>Logo</span>
      <nav>
        <a href="">timer</a>
        <a href="">history</a>
      </nav>
    </HeaderContainer>
  )
}
